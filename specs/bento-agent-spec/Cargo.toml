[package]
name = "bento-agent-spec"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
bento-protocol = { path = "../bento-protocol" }
serde_json = "1.0.149"
serde_yaml_ng = "0.10.0"
