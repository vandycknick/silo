[package]
name = "test-utils"
version = "0.1.0"
edition = "2021"
description = "Shared test utilities for Silo's Rust integration tests. Dev/test use only."

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
