[package]
name = "krun-sys"
version = "0.1.0"
edition = "2021"
links = "krun"
build = "build.rs"

[dependencies]
thiserror = "2.0.18"

[build-dependencies]
bindgen = { version = "0.72.1", optional = true }

[features]
regenerate = ["dep:bindgen"]
